# Vercel KV auto-injects these when you create a KV store and link it to the project.
# (Upstash-named vars are also accepted as a fallback.)
KV_REST_API_URL=
KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
